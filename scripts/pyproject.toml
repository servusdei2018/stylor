[project]
name = "scripts"
version = "0.1.0"
description = "Stylor scripts"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "torch>=2.11.0",
    "torchvision>=0.26.0",
]

[tool.uv]
exclude-newer = "7 days"